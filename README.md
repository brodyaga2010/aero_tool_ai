# Aero Tool AI

Интеллектуальный инструмент для анализа изображений с использованием двух моделей YOLO для более точного распознавания объектов.

## 🎯 Основные функции

- Распознавание объектов на изображениях с помощью двух моделей YOLO
- Настройка порога уверенности для фильтрации результатов
- Поддержка различных форматов загрузки:
  - Одиночные изображения
  - Несколько изображений одновременно
  - Архивы с изображениями
- Веб-интерфейс для просмотра результатов
- HTTPS-поддержка с использованием SSL-сертификатов

## 🏗️ Архитектура проекта

Проект состоит из трех основных компонентов:
1. **Frontend** (React-app) - пользовательский интерфейс
2. **Backend** (API) - REST API сервис на FastAPI
3. **Reverse Proxy** (Nginx) - маршрутизация запросов и SSL-терминация

## 🚀 Быстрый запуск

Для запуска приложения используйте Docker Compose:

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd aero_tool_ai

# Запустите приложение
docker-compose up -d
```

После запуска приложение будет доступно по адресу: https://localhost

## 📁 Структура проекта
```javascript
aero_tool_ai/
├── api/              # Backend сервис
│   ├── app/
│   │   ├── models/   # Модели YOLO детектора
│   │   └── main.py   # Точка входа FastAPI
│   ├── Dockerfile    # Docker-файл backend'а
│   └── requirements.txt
├── nginx/            # Конфигурация Nginx
│   └── nginx.conf    # Конфиг reverse proxy
├── react-app/        # Frontend приложение
│   ├── app/          # Исходный код React
│   ├── Dockerfile    # Docker-файл frontend'а
│   └── package.json
└── docker-compose.yml
```

## ⚙️ Конфигурация
### Порты и маршрутизация
 - HTTPS: https://localhost (порт 443) - основной интерфейс
 - HTTP: http://localhost (порт 80) - автоматически перенаправляется на HTTPS
 - API: Внутренний сервис на порту 8000

### SSL-сертификаты
Для HTTPS необходимо поместить SSL-сертификаты в папку nginx/ssl/:

 - `fullchain.pem` - полная цепочка сертификатов
 - `privkey.pem` - приватный ключ

### Модели YOLO
Поместите файлы моделей YOLO в директорию api/models/:
 - `model1.pt` - первая модель
 - `model2.pt` - вторая модель

## 🧪 Использование
### Веб-интерфейс
1. Откройте https://localhost в браузере
2. Выберите изображения для анализа
3. Настройте порог уверенности (по умолчанию 0.5)
4. Просмотрите результаты распознавания

### API эндпоинты
 - `POST /detect/single` - анализ одного изображения
 - `POST /detect/multiple` - анализ нескольких изображений
 - `POST /detect/archive` - анализ архива изображений
 - `POST /settings` - обновление порога уверенности
  
## 📚 Swagger документация
FastAPI автоматически генерирует интерактивную документацию API:

 - Swagger UI: После запуска сервиса доступна по адресу http://localhost:8000/docs
 - ReDoc: Альтернативная документация доступна по адресу http://localhost:8000/redoc

Документация включает:
 - Полное описание всех эндпоинтов
 - Возможность тестирования API прямо в браузере
 - Примеры запросов и ответов
 - Схемы данных запросов и ответов

Для локальной разработки документация доступна сразу после запуска сервера разработки.
  
## 🛠️ Разработка
### Backend (FastAPI)
```bash
cd api
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Запустите сервер разработки
python run.py
```

### Frontend (React)
```bash
cd react-app
# Установите зависимости
npm install

# Запустите в режиме разработки
npm run dev
```

## 📦 Docker образы
Проект использует следующие базовые образы:

 - Backend: `ultralytics/ultralytics:latest-cpu`
 - Frontend: `node:20-alpine` (для сборки) и ``nginx:alpine (для запуска)
 - Nginx: `nginx:alpine`

```javascript
Эта инструкция охватывает все основные аспекты проекта, включая его архитектуру, способы запуска, конфигурацию и использование. В ней также указаны пути к ключевым файлам и компонентам системы.
```